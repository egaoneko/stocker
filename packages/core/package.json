{
  "name": "@stocker/core",
  "version": "0.0.1",
  "description": "core",
  "main": "lib/index",
  "types": "lib/index",
  "files": [
    "lib"
  ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./lib",
    "compile": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
  },
  "devDependencies": {
    "typescript": "~3.6.4"
  }
}
